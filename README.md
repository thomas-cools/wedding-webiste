# wedding-website

This project is a simple wedding site migrated to React + Vite with TypeScript support.

Local development:

```bash
npm install
npm run dev
```

Build and preview production bundle:

```bash
npm run build
npm run preview
```

The app entry is `index.html` which loads `/src/main.tsx`.
